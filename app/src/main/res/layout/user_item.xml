<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools"
    android:paddingHorizontal="16dp"
    >

    <ImageView
        android:id="@+id/ivProfile_picture"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginEnd="16sp"
        tools:srcCompat="@tools:sample/avatars"
        />

    <TextView
        android:id="@+id/tvUser_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@id/ivProfile_picture"
        android:layout_alignBottom="@id/ivProfile_picture"
        android:layout_alignParentEnd="true"
        style="@style/TextAppearance.MaterialComponents.Headline6"
        android:layout_toEndOf="@id/ivProfile_picture"
        android:gravity="center_vertical"
        tools:text="Muhammad Rio" />

    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp"
        android:layout_below="@id/tvUser_name"
        />

</RelativeLayout>